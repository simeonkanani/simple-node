version: 2.1

workflows:
  simple-node-deploy:
    jobs:
      - test:
jobs:
  test:
    docker:
      - image: cimg/node:18.4.0
    steps:
      - checkout
      - run:
        name: install dependencies
        command: npm install
      - run:
        name: Run Tests
        command: npm run test
        
